# Log Linter

Линтер для проверки лог-записей в Go коде.

## Возможности

-  Проверка на строчную букву в начале сообщения
-  Проверка на английский язык
-  Проверка на спецсимволы и эмодзи
-  Проверка на чувствительные данные (password, token, api_key и т.д.)
-  Поддержка `log/slog` и `go.uber.org/zap`
-  Интеграция с golangci-lint
-  **Конфигурация через `.loglinter.json`**
-  **Автоматическое исправление ошибок (SuggestedFixes)**
-  **Кастомные паттерны для чувствительных данных**
-  **CI/CD для GitHub Actions и GitLab CI**

## Быстрый старт

### Установка

```bash
go install github.com/dontpanicw/log-linter/cmd/loglinter@latest
```

### Использование

```bash
loglinter ./...
```

### Автоматическое исправление

```bash
loglinter -fix ./...
```

## Примеры

### ❌ Неправильно

```go
slog.Info("Starting server on port 8080")        // Заглавная буква
slog.Error("запуск сервера")                     // Не английский язык
slog.Info("server started!🚀")                   // Эмодзи
slog.Info("user password: " + password)          // Чувствительные данные
```

### ✅ Правильно

```go
slog.Info("starting server on port 8080")
slog.Error("starting server")
slog.Info("server started")
slog.Info("user authenticated successfully")
```

## Конфигурация

Создайте файл `.loglinter.json` в корне проекта:

```json
{
  "check_lowercase": true,
  "check_english_only": true,
  "check_special_chars": true,
  "check_sensitive_data": true,
  "sensitive_keywords": [
    "password",
    "token",
    "api_key",
    "secret"
  ]
}
```

## Документация

- [Установка и использование](installation.md)
- [Интеграция с golangci-lint](installation.md#способ-2-интеграция-с-golangci-lint)
- [CI/CD настройка](installation.md#способ-3-использование-в-cicd)

## Разработка

### Сборка

```bash
make build
```

### Тестирование

```bash
make test
```

### Сборка плагина для golangci-lint

```bash
make plugin
```

## Правила проверки

1. **Строчная буква**: Лог-сообщения должны начинаться со строчной буквы
2. **Английский язык**: Лог-сообщения должны быть только на английском
3. **Без спецсимволов**: Лог-сообщения не должны содержать эмодзи или избыточную пунктуацию (!!!, ...)
4. **Без чувствительных данных**: Лог-сообщения не должны содержать password, token, api_key и другие чувствительные данные

## Поддерживаемые логгеры

- `log/slog` (стандартная библиотека)
- `go.uber.org/zap`

## CI/CD

### GitHub Actions

Проект включает готовую конфигурацию для GitHub Actions (`.github/workflows/ci.yml`):
- Автоматическое тестирование на Go 1.22 и 1.23
- Линтинг с golangci-lint
- Сборка бинарников и плагинов
- Покрытие кода
